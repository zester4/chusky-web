"use client";

import { useCallback, useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { Activity, Building2, CalendarClock, Check, Clock3, ExternalLink, Languages, LoaderCircle, Plus, RefreshCw, Save, ShieldCheck, Users, Video, X } from "lucide-react";
import { chuskyApi, type ConnectedAccount, type Meeting, type MeetingCapability, type MeetingContact, type MeetingNativeCapability, type MeetingProviderCapabilities, type MeetingRepresentativeProfile, type MeetingRoomPolicy, type MeetingWorkspace } from "@/lib/chusky-api";
import { authClient } from "@/lib/auth-client";
import { useLiveData } from "@/lib/live-sync";
import { Button, Card, PageHeading, Status } from "./app-shell";
import { ConfirmDialog } from "./confirm-dialog";

const initialProfile: MeetingRepresentativeProfile = { enabled: false, representativeName: "Chusky", organizationName: "", role: "custom", objective: "", communicationStyle: "", approvedKnowledge: "", authorityBoundaries: "", allowedComposioTools: [], composioAccountAliases: {}, allowedNativeTools: [], allowMeetingScheduling: true, autoJoinCalendar: false, updatedAt: 0 };
const inputClass = "mt-1.5 min-h-9 w-full border border-foreground/15 bg-background px-2.5 text-xs outline-none focus:border-foreground/50";
const textClass = "mt-1.5 w-full resize-y border border-foreground/15 bg-background px-2.5 py-2 text-xs leading-5 outline-none focus:border-foreground/50";

function date(value?: string) { if (!value) return "Time not provided"; const parsed = new Date(value); return Number.isNaN(parsed.valueOf()) ? "Time not provided" : new Intl.DateTimeFormat("en", { dateStyle: "medium", timeStyle: "short" }).format(parsed); }
function titleCase(value: string) { return value.toLowerCase().split(/[_-]+/).filter(Boolean).map((word) => word.charAt(0).toUpperCase() + word.slice(1)).join(" "); }
function humanToolLabel(tool: MeetingCapability) { const prefix = tool.toolkitPrefix ? `${tool.toolkitPrefix}_` : ""; const action = tool.slug.startsWith(prefix) ? tool.slug.slice(prefix.length) : tool.slug; return `${titleCase(tool.toolkit || tool.toolkitPrefix)} · ${titleCase(action)}`; }
function humanNativeLabel(tool: MeetingNativeCapability) { return titleCase(tool.slug.replace(/^CHUCK_/, "")); }
function accountPrefix(account: ConnectedAccount, tools: MeetingCapability[]) { return tools.find((tool) => tool.slug.startsWith(`${account.toolkit.replace(/[^a-zA-Z0-9]/g, "").toUpperCase()}_`))?.toolkitPrefix || account.toolkit.replace(/[^a-zA-Z0-9]/g, "").toUpperCase(); }
function runtimeLabel(value?: Meeting["runtimeState"]) { return value ? value.replaceAll("_", " ") : "status pending"; }
function capabilityLabel(value: string) { return value.replaceAll(/([a-z])([A-Z])/g, "$1 $2").replaceAll("_", " "); }
function capabilityTone(value: string) { return value === "supported" ? "text-emerald-700" : value === "limited" ? "text-amber-700" : "text-muted-foreground"; }

export function MeetingsPage() {
  const router = useRouter();
  const activeOrganization = authClient.useActiveOrganization();
  const activeWorkspace = activeOrganization.data as ({ id: string; name?: string; teams?: Array<{ id: string; name: string }> } | null | undefined);
  const organizationId = activeWorkspace?.id ?? "";
  const [workspace, setWorkspace] = useState<MeetingWorkspace>();
  const [profile, setProfile] = useState<MeetingRepresentativeProfile>(initialProfile);
  const [composioTools, setComposioTools] = useState<MeetingCapability[]>([]); const [nativeTools, setNativeTools] = useState<MeetingNativeCapability[]>([]); const [connections, setConnections] = useState<ConnectedAccount[]>([]); const [composioAvailable, setComposioAvailable] = useState(true);
  const [busy, setBusy] = useState<string>(); const [error, setError] = useState<string>(); const [notice, setNotice] = useState<string>();
  const [removeContact, setRemoveContact] = useState<MeetingContact>();
  const [roomName, setRoomName] = useState(""); const [roomDescription, setRoomDescription] = useState(""); const [roomTeamId, setRoomTeamId] = useState(""); const [roomVisibility, setRoomVisibility] = useState<MeetingRoomPolicy["visibility"]>("team"); const [roomMode, setRoomMode] = useState<MeetingRoomPolicy["defaultMode"]>("addressed"); const [roomAllowedComposioTools, setRoomAllowedComposioTools] = useState<string[]>([]); const [roomAllowedNativeTools, setRoomAllowedNativeTools] = useState<string[]>([]);
  const [meetingUrl, setMeetingUrl] = useState(""); const [selectedRoomId, setSelectedRoomId] = useState(""); const [joinMode, setJoinMode] = useState<Meeting["interactionMode"]>("addressed"); const [languageMode, setLanguageMode] = useState<NonNullable<Meeting["languageMode"]>>("english"); const [languageHints, setLanguageHints] = useState(""); const [keyterms, setKeyterms] = useState("");

  const load = useCallback(async (refreshProfile = true) => {
    setError(undefined);
    try {
      const [privateWorkspace, capabilities] = await Promise.all([chuskyApi.meetings.list(), chuskyApi.meetings.capabilities()]);
      let nextWorkspace = privateWorkspace;
      if (organizationId) {
        try {
          const shared = await chuskyApi.meetings.list(organizationId);
          const known = new Set(privateWorkspace.meetings.map((item) => item.id));
          nextWorkspace = { ...privateWorkspace, rooms: shared.rooms, meetings: [...shared.meetings.filter((item) => !known.has(item.id)), ...privateWorkspace.meetings] };
        } catch { /* Personal meetings remain usable if the active workspace is unavailable. */ }
      }
      setWorkspace(nextWorkspace);
      setComposioTools(capabilities.composioTools); setNativeTools(capabilities.nativeTools); setConnections(capabilities.connections); setComposioAvailable(capabilities.composioAvailable);
      if (refreshProfile) {
        const nextProfile = await chuskyApi.meetings.profile();
        setProfile(nextProfile);
      }
    } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not load the meeting workspace."); }
  }, [organizationId]);
  useEffect(() => { void load(); }, [load]);
  useLiveData(() => load(false), 15_000);

  const saveProfile = async () => {
    setBusy("profile"); setError(undefined); setNotice(undefined);
    try {
      const next = await chuskyApi.meetings.updateProfile(profile);
      const { autoJoinReconciliation, ...savedProfile } = next;
      setProfile(savedProfile);
      setNotice(autoJoinReconciliation
        ? `Profile saved. ${autoJoinReconciliation.cancelled} scheduled join${autoJoinReconciliation.cancelled === 1 ? "" : "s"} cancelled; ${autoJoinReconciliation.stillInCall} live meeting${autoJoinReconciliation.stillInCall === 1 ? "" : "s"} left running${autoJoinReconciliation.failures ? `; ${autoJoinReconciliation.failures} cancellation${autoJoinReconciliation.failures === 1 ? "" : "s"} need attention` : ""}.`
        : "Meeting representative profile saved.");
    } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not save this profile."); }
    finally { setBusy(undefined); }
  };
  const remove = async () => {
    if (!removeContact) return;
    setBusy(removeContact.id); setError(undefined);
    try { await chuskyApi.meetings.deleteContact(removeContact.id); setWorkspace((current) => current ? { ...current, contacts: current.contacts.filter((item) => item.id !== removeContact.id) } : current); setRemoveContact(undefined); }
    catch (cause) { setError(cause instanceof Error ? cause.message : "Could not remove this follow-up contact."); }
    finally { setBusy(undefined); }
  };
  const startJoinDraft = (preparationId: string) => {
    const prompt = `Join the calendar-prepared meeting with preparation ID ${preparationId}. Use the owner-scoped prepared-meeting join capability and its verified event brief; do not guess or ask me to paste the meeting URL. If this preparation is no longer joinable, explain why.`;
    router.push(`/app/chat?new=1&draft=${encodeURIComponent(prompt)}`);
  };
  const addComposioTool = (slug: string) => { if (!slug) return; setProfile((current) => ({ ...current, allowedComposioTools: [...new Set([...current.allowedComposioTools, slug])] })); };
  const removeComposioTool = (slug: string) => setProfile((current) => ({ ...current, allowedComposioTools: current.allowedComposioTools.filter((item) => item !== slug) }));
  const addNativeTool = (slug: string) => { if (!slug) return; setProfile((current) => ({ ...current, allowedNativeTools: [...new Set([...current.allowedNativeTools, slug])] })); };
  const removeNativeTool = (slug: string) => setProfile((current) => ({ ...current, allowedNativeTools: current.allowedNativeTools.filter((item) => item !== slug) }));
  const addRoomComposioTool = (slug: string) => { if (slug) setRoomAllowedComposioTools((current) => [...new Set([...current, slug])]); };
  const removeRoomComposioTool = (slug: string) => setRoomAllowedComposioTools((current) => current.filter((item) => item !== slug));
  const addRoomNativeTool = (slug: string) => { if (slug) setRoomAllowedNativeTools((current) => [...new Set([...current, slug])]); };
  const removeRoomNativeTool = (slug: string) => setRoomAllowedNativeTools((current) => current.filter((item) => item !== slug));
  const createRoom = async () => {
    if (!organizationId || !roomName.trim()) return;
    setBusy("room"); setError(undefined); setNotice(undefined);
    try {
      const policy: MeetingRoomPolicy = { defaultMode: roomMode, visibility: roomVisibility, allowScreenUnderstanding: false, requireApprovalForExternalActions: true, allowedComposioTools: roomAllowedComposioTools, allowedNativeTools: roomAllowedNativeTools };
      const room = await chuskyApi.meetings.rooms.create({ organizationId, name: roomName.trim(), ...(roomDescription.trim() ? { description: roomDescription.trim() } : {}), ...(roomTeamId ? { teamId: roomTeamId } : {}), policy });
      setWorkspace((current) => current ? { ...current, rooms: [room, ...(current.rooms ?? [])] } : current); setRoomName(""); setRoomDescription(""); setRoomAllowedComposioTools([]); setRoomAllowedNativeTools([]); setNotice(`Meeting room “${room.name}” created.`);
    } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not create the meeting room."); }
    finally { setBusy(undefined); }
  };
  const joinMeeting = async () => {
    if (!meetingUrl.trim()) return;
    const hints = languageHints.split(",").map((item) => item.trim()).filter(Boolean).slice(0, 8);
    const terms = keyterms.split(",").map((item) => item.trim()).filter(Boolean).slice(0, 50);
    if (languageMode === "multilingual" && hints.length === 0) { setError("Choose at least one language hint for multilingual meeting audio."); return; }
    setBusy("join"); setError(undefined); setNotice(undefined);
    try {
      const meeting = await chuskyApi.meetings.join({ meetingUrl: meetingUrl.trim(), interactionMode: joinMode, languageMode, ...(hints.length ? { languageHints: hints } : {}), ...(terms.length ? { keyterms: terms } : {}), ...(selectedRoomId ? { roomId: selectedRoomId } : {}) });
      setWorkspace((current) => current ? { ...current, meetings: [meeting, ...current.meetings.filter((item) => item.id !== meeting.id)] } : current); setMeetingUrl(""); setLanguageHints(""); setKeyterms(""); setNotice("Chusky is joining the meeting. The session will appear below as its provider status changes.");
    } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not join this meeting."); }
    finally { setBusy(undefined); }
  };
  const addAccountAlias = (value: string) => {
    if (!value) return;
    const [prefix, accountId] = value.split("::");
    const account = connections.find((item) => item.id === accountId);
    if (!prefix || !account) return;
    setProfile((current) => ({ ...current, composioAccountAliases: { ...current.composioAccountAliases, [prefix]: account.alias || account.id } }));
  };
  const removeAccountAlias = (prefix: string) => setProfile((current) => { const next = { ...current.composioAccountAliases }; delete next[prefix]; return { ...current, composioAccountAliases: next }; });
  const activeConnections = connections.filter((item) => item.status.toUpperCase() === "ACTIVE");
  const aliasOptions = activeConnections.map((account) => ({ account, prefix: accountPrefix(account, composioTools) }));
  const selectedToolSet = new Set(profile.allowedComposioTools);
  const selectedNativeSet = new Set(profile.allowedNativeTools);
  const rooms = workspace?.rooms ?? [];

  return <>
    <PageHeading eyebrow="Calendar & live sessions" title="Meetings" description="Review calendar meeting preparations, participant context, live-call outcomes, and the representative profile Chusky uses. Calendar triggers can also describe non-meeting events; only verified supported video links become meeting preparations." action={<Button secondary onClick={() => void load()}><span className="hidden sm:inline-flex"><RefreshCw size={13}/></span> Refresh</Button>} />
    {error && <div role="alert" className="mb-4 border border-amber-300 bg-amber-50 p-3 text-xs text-amber-950">{error}</div>}{notice && <p role="status" className="mb-3 text-xs text-emerald-700">{notice}</p>}

    <section className="mb-5"><div className="mb-2 flex items-end justify-between gap-3"><div><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-muted-foreground">Workspace meeting rooms</p><h2 className="mt-1 font-display text-2xl">Bring a team into the room</h2></div><span className="text-[10px] text-muted-foreground">Shared metadata · private owner context</span></div>
      {!organizationId ? <Card className="p-4 text-xs leading-5 text-muted-foreground"><Building2 size={15} className="mb-2"/>Create or select a workspace in Organizations to create department rooms for Marketing, Finance, Sales, or any other team. Personal meetings continue to work without a workspace.</Card> : <div className="grid gap-3 xl:grid-cols-[minmax(0,1fr)_minmax(0,1.15fr)]"><Card className="p-4 sm:p-5"><div className="flex items-start gap-3"><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-foreground text-background"><Building2 size={15}/></span><div><p className="text-sm font-medium">{activeWorkspace?.name || "Active workspace"}</p><p className="mt-1 text-[11px] leading-5 text-muted-foreground">A room defines who can see meeting outcomes and which department policy applies. Participant speech never becomes workspace identity.</p></div></div><div className="mt-4 space-y-3"><label className="block text-xs text-muted-foreground">Room name<input value={roomName} onChange={(event) => setRoomName(event.target.value)} placeholder="Marketing weekly" maxLength={120} className={inputClass}/></label><label className="block text-xs text-muted-foreground">Purpose<textarea value={roomDescription} onChange={(event) => setRoomDescription(event.target.value)} placeholder="Shared outcomes and follow-ups for the marketing team" rows={2} maxLength={1000} className={textClass}/></label><div className="grid gap-3 sm:grid-cols-3"><label className="block text-xs text-muted-foreground">Department team<select value={roomTeamId} onChange={(event) => { setRoomTeamId(event.target.value); if (event.target.value) setRoomVisibility("team"); }} className={inputClass}><option value="">Workspace-wide</option>{activeWorkspace?.teams?.map((team) => <option key={team.id} value={team.id}>{team.name}</option>)}</select></label><label className="block text-xs text-muted-foreground">Visibility<select value={roomVisibility} onChange={(event) => setRoomVisibility(event.target.value as MeetingRoomPolicy["visibility"])} className={inputClass}><option value="team" disabled={!roomTeamId}>Team members</option><option value="organization">Everyone in workspace</option><option value="private">Room creator only</option></select></label><label className="block text-xs text-muted-foreground">Default behavior<select value={roomMode} onChange={(event) => setRoomMode(event.target.value as MeetingRoomPolicy["defaultMode"])} className={inputClass}><option value="addressed">Answer when addressed</option><option value="copilot">Proactive copilot</option><option value="representative">Company representative</option></select></label></div><div className="mt-3 space-y-2"><CapabilityPicker label="Room connected-app actions" hint="Optional: grant only the exact connected actions this department room may use." options={composioTools} selected={roomAllowedComposioTools} onAdd={addRoomComposioTool} onRemove={removeRoomComposioTool} renderOption={(option) => humanToolLabel(option as MeetingCapability)} empty="No connected actions available."/><CapabilityPicker label="Room Chusky actions" hint="Room grants are intersected with the private representative profile before execution." options={nativeTools} selected={roomAllowedNativeTools} onAdd={addRoomNativeTool} onRemove={removeRoomNativeTool} renderOption={(option) => humanNativeLabel(option as MeetingNativeCapability)} empty="No native actions available."/></div><Button className="mt-3" disabled={busy === "room" || !roomName.trim() || (roomVisibility === "team" && !roomTeamId)} onClick={() => void createRoom()}>{busy === "room" ? <LoaderCircle size={13} className="animate-spin"/> : <Plus size={13}/>} Create room</Button></div></Card><Card className="p-4 sm:p-5"><div className="flex items-center justify-between gap-3"><div><p className="text-sm font-medium">Join a live meeting</p><p className="mt-1 text-[11px] text-muted-foreground">Paste a verified Google Meet, Zoom, Teams, or Webex link and choose the department room.</p></div><Video size={17} className="text-muted-foreground"/></div><div className="mt-4 space-y-3"><label className="block text-xs text-muted-foreground">Meeting link<input value={meetingUrl} onChange={(event) => setMeetingUrl(event.target.value)} placeholder="https://meet.google.com/..." className={inputClass}/></label><div className="grid gap-3 sm:grid-cols-2"><label className="block text-xs text-muted-foreground">Shared room<select value={selectedRoomId} onChange={(event) => setSelectedRoomId(event.target.value)} className={inputClass}><option value="">Personal meeting</option>{workspace?.rooms.map((room) => <option key={room.id} value={room.id}>{room.name} · {room.policy.visibility}</option>)}</select></label><label className="block text-xs text-muted-foreground">Interaction<select value={joinMode} onChange={(event) => setJoinMode(event.target.value as Meeting["interactionMode"])} className={inputClass}><option value="addressed">Answer when addressed</option><option value="copilot">Proactive copilot</option><option value="representative">Company representative</option></select></label></div><div className="grid gap-3 sm:grid-cols-2"><label className="block text-xs text-muted-foreground">Meeting audio<select value={languageMode} onChange={(event) => setLanguageMode(event.target.value as NonNullable<Meeting["languageMode"]>)} className={inputClass}><option value="english">English</option><option value="multilingual">Multilingual (Flux multi)</option></select></label><label className="block text-xs text-muted-foreground">Language hints<span className="mt-1.5 flex items-center gap-1.5 border border-foreground/15 bg-background px-2.5"><Languages size={13} className="shrink-0 text-muted-foreground"/><input value={languageHints} onChange={(event) => setLanguageHints(event.target.value)} placeholder="en, es (required for multilingual)" className="min-h-9 min-w-0 flex-1 bg-transparent text-xs outline-none"/></span></label></div><label className="block text-xs text-muted-foreground">Names and product terms<span className="mt-1.5 flex items-center gap-1.5 border border-foreground/15 bg-background px-2.5"><input value={keyterms} onChange={(event) => setKeyterms(event.target.value)} placeholder="Chusky, Acme (optional, comma-separated)" className="min-h-9 min-w-0 flex-1 bg-transparent px-2.5 text-xs outline-none"/></span></label><p className="text-[10px] leading-4 text-muted-foreground">Hints are sent to Deepgram for turn recognition. Key terms improve recognition of company names and product language; they are not used as authorization.</p><Button disabled={busy === "join" || !meetingUrl.trim() || (languageMode === "multilingual" && !languageHints.trim())} onClick={() => void joinMeeting()}>{busy === "join" ? <LoaderCircle size={13} className="animate-spin"/> : <Video size={13}/>} Join meeting</Button></div></Card></div>}
      {rooms.length ? <div className="mt-3 grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{rooms.map((room) => <Card key={room.id} className="p-3"><div className="flex items-start justify-between gap-2"><div><p className="text-xs font-medium">{room.name}</p><p className="mt-1 text-[10px] text-muted-foreground">{room.teamId ? "Department team" : "Workspace-wide"} · {room.policy.defaultMode} · {room.meetingCount} session{room.meetingCount === 1 ? "" : "s"}</p></div><Status tone={room.policy.visibility === "private" ? "gray" : "green"}>{room.policy.visibility}</Status></div>{room.description && <p className="mt-2 text-[11px] leading-4 text-muted-foreground">{room.description}</p>}</Card>)}</div> : null}
    </section>

    <section className="mb-5"><div className="mb-2 flex items-end justify-between gap-3"><div><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-muted-foreground">Calendar briefings</p><h2 className="mt-1 font-display text-2xl">Prepared events</h2></div><span className="text-[10px] text-muted-foreground">Refreshes every 15 seconds</span></div>
      {!workspace ? <Card className="flex items-center gap-2 p-4 text-xs text-muted-foreground"><LoaderCircle size={14} className="animate-spin"/> Loading meeting data…</Card> : workspace.preparations.length ? <div className="grid gap-3">{workspace.preparations.map((item) => <Card key={item.id} className="p-4 sm:p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><CalendarClock size={15} className="text-muted-foreground"/><h3 className="break-words text-sm font-medium">{item.title || "Calendar event"}</h3><Status tone={item.status === "cancelled" || item.status === "expired" ? "gray" : item.status === "prepared" || item.status === "auto_scheduled" ? "green" : "amber"}>{item.status.replaceAll("_", " ")}</Status></div><p className="mt-1.5 text-[11px] text-muted-foreground">{date(item.startAt)}{item.endAt ? ` – ${date(item.endAt)}` : ""} · {item.lifecycle.replaceAll("_", " ")}</p></div>{(item.status === "prepared" || item.status === "auto_scheduled") && item.meetingUrlAvailable && <Button onClick={() => startJoinDraft(item.id)}>Ask Chusky to join</Button>}</div>
        {item.participants.length > 0 && <p className="mt-3 flex items-start gap-2 text-[11px] text-muted-foreground"><Users size={13} className="mt-0.5 shrink-0"/><span>{item.participants.join(", ")}</span></p>}
        <div className="mt-3 border-t border-foreground/10 pt-3"><p className="font-mono text-[9px] uppercase tracking-[0.15em] text-muted-foreground">Meeting brief</p>{item.brief ? <p className="mt-1.5 whitespace-pre-wrap text-xs leading-5">{item.brief}</p> : <p className="mt-1.5 text-[11px] text-muted-foreground">{item.briefStatus === "processing" || item.briefStatus === "queued" ? "Brief is being prepared…" : item.status === "cancelled" ? "This event was cancelled; it is not a joinable meeting." : item.meetingUrlAvailable ? "No completed brief is available yet. Chusky can still review the verified event when you ask it to join." : "No supported meeting link was found. This calendar event is not treated as a meeting."}</p>}</div>
        <p className="mt-2 break-all font-mono text-[9px] text-muted-foreground">Preparation {item.id}{item.calendarEventId ? ` · Event ${item.calendarEventId}` : ""}</p>
      </Card>)}</div> : <Card className="p-4 text-xs leading-5 text-muted-foreground">No calendar meeting preparations yet. Calendar triggers remain useful for ordinary events too; only events with a verified supported conferencing link appear here as meetings.</Card>}
    </section>

    <section className="mb-5"><div className="mb-2"><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-muted-foreground">Recall sessions</p><h2 className="mt-1 font-display text-2xl">Recent meetings</h2></div>{!workspace ? null : workspace.meetings.length ? <div className="grid gap-3">{workspace.meetings.map((meeting) => <MeetingCard key={meeting.id} meeting={meeting}/>)}</div> : <Card className="p-4 text-xs text-muted-foreground">No Recall meeting sessions recorded for this workspace.</Card>}</section>

    <section className="mb-5"><div className="mb-2"><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-muted-foreground">Follow-up queue</p><h2 className="mt-1 font-display text-2xl">Interested participants</h2></div>{!workspace ? null : workspace.contacts.length ? <Card>{workspace.contacts.map((contact) => <div key={contact.id} className="flex flex-col gap-2.5 border-b border-foreground/10 p-4 last:border-0 sm:flex-row sm:items-start"><div className="min-w-0 flex-1"><p className="text-xs font-medium">{contact.participantName}</p><p className="mt-1 break-words text-[11px] text-muted-foreground">{[contact.email, contact.phone, contact.contactPreference].filter(Boolean).join(" · ")}</p><p className="mt-2 text-xs leading-5">{contact.interest}</p>{contact.nextStep && <p className="mt-1 text-[11px] text-muted-foreground">Next: {contact.nextStep}</p>}{contact.followUpAt && <p className="mt-1 text-[10px] text-muted-foreground">Follow-up {date(contact.followUpAt)}</p>}</div><Button secondary disabled={busy === contact.id} onClick={() => setRemoveContact(contact)}>Remove</Button></div>)}</Card> : <Card className="p-4 text-xs text-muted-foreground">No participant follow-ups have been recorded yet.</Card>}</section>

    <section><div className="mb-2"><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-muted-foreground">How Chusky represents you</p><h2 className="mt-1 font-display text-2xl">Meeting profile</h2></div><Card className="p-4 sm:p-5"><p className="mb-4 text-xs leading-5 text-muted-foreground">This profile guides the meeting agent’s style and available tools. It does not make a calendar event joinable unless a verified meeting link exists. Connected app aliases refer to exact accounts you choose to make available.</p>
      <div className="grid gap-3 sm:grid-cols-2"><label className="flex min-h-10 items-center gap-2 text-xs"><input type="checkbox" checked={profile.enabled} onChange={(event) => setProfile({ ...profile, enabled: event.target.checked })} className="accent-foreground"/> Enable representative profile</label><label className="block text-xs text-muted-foreground">Representative name<input value={profile.representativeName} onChange={(event) => setProfile({ ...profile, representativeName: event.target.value })} maxLength={80} className={inputClass}/></label><label className="block text-xs text-muted-foreground">Organization<input value={profile.organizationName} onChange={(event) => setProfile({ ...profile, organizationName: event.target.value })} maxLength={120} className={inputClass}/></label><label className="block text-xs text-muted-foreground">Meeting role<select value={profile.role} onChange={(event) => setProfile({ ...profile, role: event.target.value as MeetingRepresentativeProfile["role"] })} className={inputClass}><option value="sales">Sales</option><option value="client_onboarding">Client onboarding</option><option value="employee_onboarding">Employee onboarding</option><option value="customer_success">Customer success</option><option value="custom">Custom</option></select></label></div>
      <label className="mt-3 block text-xs text-muted-foreground">General objective<textarea value={profile.objective} onChange={(event) => setProfile({ ...profile, objective: event.target.value })} rows={2} maxLength={1500} className={textClass}/></label><label className="mt-3 block text-xs text-muted-foreground">Communication style<textarea value={profile.communicationStyle} onChange={(event) => setProfile({ ...profile, communicationStyle: event.target.value })} rows={2} maxLength={1500} className={textClass}/></label><label className="mt-3 block text-xs text-muted-foreground">Approved company / product knowledge<textarea value={profile.approvedKnowledge} onChange={(event) => setProfile({ ...profile, approvedKnowledge: event.target.value })} rows={4} maxLength={12000} className={textClass}/></label><label className="mt-3 block text-xs text-muted-foreground">Authority guidance<textarea value={profile.authorityBoundaries} onChange={(event) => setProfile({ ...profile, authorityBoundaries: event.target.value })} rows={3} maxLength={3000} className={textClass}/></label>
      <div className="mt-3 grid gap-3 sm:grid-cols-2">
        <CapabilityPicker label="Allowed connected-app actions" hint={composioAvailable ? "Choose verified, low-risk actions from connected Composio accounts." : "Connected-app capabilities are temporarily unavailable."} options={composioTools} selected={profile.allowedComposioTools} onAdd={addComposioTool} onRemove={removeComposioTool} renderOption={(option) => humanToolLabel(option as MeetingCapability)} empty="No safe connected-app actions are available yet." />
        <AccountAliasPicker options={aliasOptions} selected={profile.composioAccountAliases} onAdd={addAccountAlias} onRemove={removeAccountAlias} onOpenApps={() => router.push("/app/apps")} />
      </div>
      <div className="mt-3"><CapabilityPicker label="Allowed Chusky actions" hint="These native actions are limited to the meeting representative allowlist." options={nativeTools} selected={profile.allowedNativeTools} onAdd={addNativeTool} onRemove={removeNativeTool} renderOption={(option) => humanNativeLabel(option as MeetingNativeCapability)} empty="No native meeting actions are available." /></div>
      {(selectedToolSet.size > 0 || selectedNativeSet.size > 0) && <p className="mt-3 flex items-center gap-1.5 text-[10px] text-emerald-700"><Check size={12}/> Only the actions shown above will be available to this representative. Saving updates the private backend profile.</p>}
      <div className="mt-4 flex flex-wrap gap-x-5 gap-y-3 border-t border-foreground/10 pt-4"><Toggle label="Allow calendar scheduling tools" checked={profile.allowMeetingScheduling} onChange={(value) => setProfile({ ...profile, allowMeetingScheduling: value })}/><Toggle label="Automatically join eligible calendar meetings" checked={profile.autoJoinCalendar} onChange={(value) => setProfile({ ...profile, autoJoinCalendar: value })}/></div><p className="mt-2 text-[10px] leading-4 text-muted-foreground">Automatic joining applies only to eligible calendar meetings when the existing profile and calendar rules permit it. A regular calendar trigger is not blanket join authorization.</p>
      <div className="mt-4 flex flex-wrap items-center gap-3"><Button disabled={busy === "profile"} onClick={() => void saveProfile()}>{busy === "profile" ? <LoaderCircle size={13} className="animate-spin"/> : <Save size={13}/>} Save meeting profile</Button><span className="text-[10px] text-muted-foreground">Saved to your private Chusky account.</span></div>
    </Card></section>
    {removeContact && <ConfirmDialog open onOpenChange={(open) => !open && setRemoveContact(undefined)} title={`Remove ${removeContact.participantName} from follow-ups?`} description="This removes the saved contact record from your private meeting follow-up list." confirmLabel="Remove contact" destructive onConfirm={remove}/>}
  </>;
}

function Toggle({ label, checked, onChange }: { label: string; checked: boolean; onChange: (value: boolean) => void }) { return <label className="flex items-center gap-2 text-xs"><input type="checkbox" checked={checked} onChange={(event) => onChange(event.target.checked)} className="accent-foreground"/>{label}</label>; }

type CapabilityOption = MeetingCapability | MeetingNativeCapability;

function CapabilityPicker({ label, hint, options, selected, onAdd, onRemove, renderOption, empty }: { label: string; hint: string; options: CapabilityOption[]; selected: string[]; onAdd: (slug: string) => void; onRemove: (slug: string) => void; renderOption: (option: CapabilityOption) => string; empty: string }) {
  const connected = options.filter((option) => !("connected" in option) || option.connected);
  const unavailable = options.filter((option) => "connected" in option && !option.connected);
  return <div className="border border-foreground/10 p-3">
    <label className="block text-xs font-medium text-foreground">{label}<select aria-label={label} value="" onChange={(event) => onAdd(event.target.value)} className={inputClass}><option value="">Choose an action…</option>{connected.length ? <optgroup label="Available now">{connected.filter((option) => !selected.includes(option.slug)).map((option) => <option key={option.slug} value={option.slug}>{renderOption(option)}</option>)}</optgroup> : <option disabled>{empty}</option>}{unavailable.length ? <optgroup label="Unavailable · connect the app first">{unavailable.map((option) => <option key={option.slug} value={option.slug} disabled>{renderOption(option)} · not connected</option>)}</optgroup> : null}</select></label>
    <p className="mt-1.5 text-[10px] leading-4 text-muted-foreground">{hint}</p>
    <div className="mt-2 flex flex-wrap gap-1.5">{selected.length ? selected.map((slug) => { const option = options.find((item) => item.slug === slug); return <span key={slug} className="inline-flex max-w-full items-center gap-1 border border-foreground/15 bg-foreground/[0.03] px-2 py-1 text-[10px] text-foreground"><span className="max-w-[18rem] truncate">{option ? renderOption(option) : `${slug} · saved action`}</span><button type="button" aria-label={`Remove ${slug}`} onClick={() => onRemove(slug)} className="shrink-0 text-muted-foreground hover:text-foreground"><X size={12}/></button></span>; }) : <span className="text-[10px] text-muted-foreground">Nothing selected.</span>}</div>
  </div>;
}

function AccountAliasPicker({ options, selected, onAdd, onRemove, onOpenApps }: { options: Array<{ account: ConnectedAccount; prefix: string }>; selected: Record<string, string>; onAdd: (value: string) => void; onRemove: (prefix: string) => void; onOpenApps: () => void }) {
  return <div className="border border-foreground/10 p-3">
    <label className="block text-xs font-medium text-foreground">Connected account routing<select aria-label="Connected account routing" value="" onChange={(event) => onAdd(event.target.value)} className={inputClass}><option value="">Choose a connected account…</option>{options.length ? options.map(({ account, prefix }) => <option key={`${prefix}::${account.id}`} value={`${prefix}::${account.id}`}>{titleCase(account.toolkit)} · {account.alias || account.id} · {prefix}</option>) : <option disabled>No connected accounts found</option>}</select></label>
    <p className="mt-1.5 text-[10px] leading-4 text-muted-foreground">Chusky applies the selected account to matching actions. Credentials stay inside Composio.</p>
    <div className="mt-2 space-y-1.5">{Object.entries(selected).length ? Object.entries(selected).map(([prefix, alias]) => <div key={prefix} className="flex items-center gap-2 border border-foreground/10 px-2 py-1.5 text-[10px]"><span className="font-mono text-muted-foreground">{prefix}</span><span className="min-w-0 flex-1 truncate">{alias}</span><button type="button" aria-label={`Remove ${prefix} account routing`} onClick={() => onRemove(prefix)} className="text-muted-foreground hover:text-foreground"><X size={12}/></button></div>) : <p className="text-[10px] text-muted-foreground">No account routing selected.</p>}</div>
    <button type="button" onClick={onOpenApps} className="mt-2 inline-flex items-center gap-1 text-[10px] text-muted-foreground underline underline-offset-2 hover:text-foreground">Connect another app <ExternalLink size={11}/></button>
  </div>;
}

function capabilityEntries(capabilities?: MeetingProviderCapabilities) {
  if (!capabilities) return [];
  return ["audio", "outboundChat", "inboundChat", "screenShare", "participantEvents", "speakerEvents", "scheduledJoin", "waitingRoom", "admissionControl", "transcript"].map((key) => ({ key, value: capabilities[key as keyof MeetingProviderCapabilities] as string })).filter((entry) => typeof entry.value === "string");
}

function MeetingCard({ meeting }: { meeting: Meeting }) {
  const metrics = meeting.turnMetrics;
  const escalation = meeting.outcome?.escalation;
  return <Card className="p-4 sm:p-5"><div className="flex flex-wrap items-center gap-2"><h3 className="text-sm font-medium">{meeting.title || `${meeting.platform} meeting`}</h3><Status tone={meeting.status === "in_call" || meeting.status === "joining" ? "green" : meeting.error ? "amber" : "gray"}>{meeting.status.replaceAll("_", " ")}</Status><Status tone={meeting.runtimeState === "degraded" || meeting.runtimeState === "voice_unavailable" ? "amber" : meeting.runtimeState === "healthy" ? "green" : "gray"}>{runtimeLabel(meeting.runtimeState)}</Status><span className="text-[10px] text-muted-foreground">{meeting.interactionMode} · {date(meeting.joinAt || meeting.createdAt)}</span></div>
    {(meeting.languageMode || meeting.languageHints?.length || meeting.keyterms?.length) && <p className="mt-2 flex flex-wrap items-center gap-1.5 text-[10px] text-muted-foreground"><Languages size={12}/>{meeting.languageMode === "multilingual" ? `Multilingual · ${(meeting.languageHints ?? []).join(", ")}` : "English audio"}{meeting.keyterms?.length ? ` · ${meeting.keyterms.length} keyterm${meeting.keyterms.length === 1 ? "" : "s"}` : ""}</p>}
    {meeting.error && <p className="mt-2 text-xs text-amber-800">{meeting.error}</p>}
    {metrics && <div className="mt-3 grid grid-cols-2 gap-2 border-y border-foreground/10 py-3 text-[10px] sm:grid-cols-4"><div><p className="text-muted-foreground">Turns</p><p className="mt-1 font-medium">{metrics.turns}</p></div><div><p className="text-muted-foreground">Completed</p><p className="mt-1 font-medium">{metrics.completedTurns}</p></div><div><p className="text-muted-foreground">Fallbacks</p><p className="mt-1 font-medium">{metrics.fallbackTurns}</p></div><div><p className="text-muted-foreground">First audio</p><p className="mt-1 font-medium">{metrics.firstAudio?.averageMs ? `${Math.round(metrics.firstAudio.averageMs)} ms avg` : "Not recorded"}</p></div></div>}
    {meeting.participantRoster.length > 0 && <div className="mt-3"><p className="flex items-center gap-1.5 font-mono text-[9px] uppercase tracking-[0.15em] text-muted-foreground"><Users size={12}/> Participants</p><p className="mt-1.5 text-[11px]">{meeting.participantRoster.map((item) => `${item.name}${item.isHost ? " (host)" : ""}${item.status === "left" ? " · left" : ""}`).join(" · ")}</p></div>}
    {meeting.capabilities && <details className="mt-3 border-t border-foreground/10 pt-3"><summary className="cursor-pointer list-none text-[10px] font-medium"><span className="inline-flex items-center gap-1.5"><ShieldCheck size={12}/> Provider capability matrix</span></summary><div className="mt-2 grid grid-cols-2 gap-x-4 gap-y-1.5 text-[10px] sm:grid-cols-3">{capabilityEntries(meeting.capabilities).map(({ key, value }) => <div key={key} className="flex items-center justify-between gap-2"><span className="text-muted-foreground">{capabilityLabel(key)}</span><span className={capabilityTone(value)}>{value}</span></div>)}</div>{meeting.capabilities.notes.length > 0 && <p className="mt-2 text-[10px] leading-4 text-muted-foreground">{meeting.capabilities.notes.join(" ")}</p>}</details>}
    {meeting.timeline?.length ? <details className="mt-3 border-t border-foreground/10 pt-3"><summary className="cursor-pointer list-none text-[10px] font-medium"><span className="inline-flex items-center gap-1.5"><Activity size={12}/> Runtime timeline · {meeting.timeline.length} events</span></summary><div className="mt-2 space-y-2">{meeting.timeline.slice(-6).reverse().map((event) => <div key={event.id} className="flex gap-2 text-[10px]"><Clock3 size={12} className="mt-0.5 shrink-0 text-muted-foreground"/><span className="text-muted-foreground">{date(event.at)}</span><span>{event.summary}</span></div>)}</div></details> : null}
    {meeting.history.length > 0 && <div className="mt-3 max-h-48 space-y-2 overflow-y-auto border-t border-foreground/10 pt-3">{meeting.history.slice(-6).map((item, index) => <p key={`${item.createdAt ?? index}-${index}`} className="text-[11px] leading-5"><span className="font-medium">{item.role === "assistant" ? "Chusky" : "Meeting context"}: </span>{item.content}</p>)}</div>}
    {meeting.outcome && <div className="mt-3 border-t border-foreground/10 pt-3"><p className="text-xs font-medium">Outcome · {meeting.outcome.title}</p><p className="mt-1 text-[11px] leading-5 text-muted-foreground">{meeting.outcome.summary}</p>{meeting.outcome.decisions.length > 0 && <p className="mt-2 text-[11px]">Decisions: {meeting.outcome.decisions.join(" · ")}</p>}{meeting.outcome.actionItems.length > 0 && <ul className="mt-2 list-disc space-y-1 pl-4 text-[11px] text-muted-foreground">{meeting.outcome.actionItems.map((item, index) => <li key={`${item.task}-${index}`}>{item.task} — {item.owner}{item.dueDate ? ` · ${item.dueDate}` : ""}</li>)}</ul>}{escalation?.required && <div className="mt-3 border border-amber-300 bg-amber-50 p-3 text-[11px] text-amber-950"><p className="font-medium">Owner review after meeting · {escalation.severity}</p>{escalation.reason && <p className="mt-1 leading-4">{escalation.reason}</p>}{escalation.nextSteps.length > 0 && <ul className="mt-2 list-disc space-y-1 pl-4">{escalation.nextSteps.map((step, index) => <li key={`${step.action}-${index}`}>{step.action}{step.owner ? ` — ${step.owner}` : ""}{step.dueDate ? ` · ${step.dueDate}` : ""}</li>)}</ul>}{escalation.openQuestions.length > 0 && <p className="mt-2">Open questions: {escalation.openQuestions.join(" · ")}</p>}<p className="mt-2 text-[10px]">This review is intentionally delivered after the meeting; Chusky does not escalate live conversations directly.</p></div>}<p className="mt-2 text-[10px] text-muted-foreground">Follow-through: {meeting.outcomeStatus || "pending"}{meeting.outcomeNotificationStatus ? ` · notification ${meeting.outcomeNotificationStatus}` : ""}</p></div>}</Card>;
}
